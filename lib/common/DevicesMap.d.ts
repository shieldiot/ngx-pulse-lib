import { MapBounds } from '../common';
import { DeviceNode } from '../common';
export declare class DevicesMap {
    deviceNodes: DeviceNode[];
    mapBounds: MapBounds;
    constructor(deviceNodes?: DeviceNode[], mapBounds?: MapBounds);
}
