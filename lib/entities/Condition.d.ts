export declare class Condition {
    field: string;
    operator: string;
    values: number[];
    strings: string[];
    constructor(field?: string, operator?: string, values?: number[], strings?: string[]);
}
